module.exports = {
  server: {
    port: 3000
  },
  app: {
    title: 'Example app'
  },
  lyra: {
    apiHost: '', // URL to Saga endpoint
    dataset: ''  // Public dataset (same as Venue dataset name suffixed with "--pub"
  }
}
